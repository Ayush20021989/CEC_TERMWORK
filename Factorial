#include<iostream>
using namespace std;
void fact(int n)
{
    int f=1;
    int temp=n;
    while(n>1)
    {
        f=f*n;
        n--;
    }
    cout<<"Factorial of "<<temp<<" is "<<f;
}
int main()
{
    int n;
    cout<<"Enter the number:";
    cin>>n;
    fact(n);
    return 0;
}
